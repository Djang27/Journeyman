line-length = 100
# Vercel's runtime. Note this is ahead of the Python 3.9 currently installed
# locally -- see the version-pinning note before relying on any 3.10+ syntax.
target-version = "py312"

[lint]
select = ["E", "F", "I", "UP", "B"]
